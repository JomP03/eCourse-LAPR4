package boardmanagement.domain;

public enum BoardPermissionType{

    // User is the owner of the board
    OWNER,
    // User can read the board
    READ,
    // User can write on the board
    WRITE
}
